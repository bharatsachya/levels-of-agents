## Stock Predictino app

To run this application, you will need to install the following dependencies:

```bash
pip install yfinance
# Other dependencies would be listed here if present in requirements.txt besides yfinance
```

This update addresses recent bug fixes and enhancements in the prediction logic (e.g., in `wise_cnn.py`).