## Stock Predictino app

### Setup Instructions

To run this application, ensure you have Python installed and then install the required dependencies using pip:

```bash
pip install -r requirements.txt
```

**Dependencies:**
*   `yfinance`: Used for fetching historical market data.

...
